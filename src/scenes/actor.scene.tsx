import * as React from 'react';
import { ActorContainer } from 'pods/actor';
import { AppLayout } from 'layouts';

export const ActorScene = () => {
  return (
    <AppLayout>
      <ActorContainer />
    </AppLayout>
  );
};
